# 扫雷 v2.2 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 落地 v2.2 五项:生成器修补式重构(28×44 过 500ms 闸门)、关卡表 21-50 恢复高瘦大盘、无尽模式(递进连胜)、全屏棋盘+液态玻璃悬浮菜单、插旗音效;规格见 `docs/superpowers/specs/2026-07-09-v2.2-design.md`。

**Architecture:** TypeScript + Vite 无框架单页;核心逻辑(core/)纯函数可测,UI(ui/)以 jsdom 单测覆盖。生成器改为"撒雷→全程推演→卡死时只在未开区重撒→从头重推"的修补循环,返回盘必过与现行一字不差的 `isSolvable` 推演。游戏页改全屏视口+绝对定位悬浮栏,视口数学引入上下净空。存档升 v3(endless 字段,21-50 成绩迁移即弃)。

**Tech Stack:** TypeScript 严格模式、Vitest(+jsdom)、vite-plugin-pwa、Tauri 2(桌面打包)。

## Global Constraints

- 工作分支:`v2.2-update`(从 main 创建;执行前用 using-git-worktrees 技能建隔离工作区亦可)。
- 所有 npm/npx 命令必须在 `E:\DIY PROJECTS\minesweeper` 目录内执行(仓库外 npx 会解析到缺 jsdom 的缓存副本)。
- 每个提交信息末尾追加第二个 `-m "Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"`。
- 1-20 关 `levels.ts` 数据字节冻结;`audio.ts` 现有五音参数禁改(新增旗音参数同样为"试听拍板,禁改",必须按本计划逐字实现)。
- 求解器**推理规则集冻结**:不得新增/删除推理规则,只许做不改变推导结果的性能优化。
- 修补式生成必须**从首格从头重推**,不得续用修补前的推理进度。
- 任务完成标准:该任务新旧测试全绿 + `npx tsc --noEmit` 干净。
- 全量验收含 GEN_BENCH 闸门:`GEN_BENCH=1 npx vitest run tests/gen-bench.test.ts` 全关 worst-of-5-seeds <500ms(PowerShell 下:`$env:GEN_BENCH="1"; npx vitest run tests/gen-bench.test.ts`;跑完 `Remove-Item Env:GEN_BENCH`)。
- 基线:main @ 204a73d(v2.1.0 + v2.2 规格文档),现有测试 150 passed | 30 skipped。

---

### Task 1: 求解器子集推理提速(乙,语义不变)

**Files:**
- Modify: `src/core/solver.ts:72-86`(step() 的子集推理段)
- Test: 既有 `tests/solver.test.ts`、`tests/generator.test.ts`(回归门,不新增测试)

**Interfaces:**
- Consumes: 无(自包含重构)
- Produces: `isSolvable(board, firstIdx, maxComponent?)` 行为与推导结果完全不变

这是纯性能重构:子集推理现在对全部约束对做 O(C²) 检查、每对新建一个 `Set`。改为"A ⊆ C 必共享 A 的首格 → 只对共享首格的约束对检查;成员集每约束每轮建一次"。推导结果集完全一致(所有子集对都会被枚举到,非子集对只是提前跳过)。

- [ ] **Step 1: 基线确认**

Run: `npx vitest run tests/solver.test.ts tests/generator.test.ts`
Expected: 全绿(记录用时作对比参考)。

- [ ] **Step 2: 替换子集推理段**

将 `src/core/solver.ts` 中这段:

```ts
  // 子集推理：A ⊆ B → (B−A) 的雷数 = B.mines − A.mines
  if (toReveal.size === 0 && toFlag.size === 0) {
    for (const a of constraints) {
      for (const c of constraints) {
        if (a === c || a.cells.length >= c.cells.length) continue;
        const cSet = new Set(c.cells);
        if (!a.cells.every((x) => cSet.has(x))) continue;
        const aSet = new Set(a.cells);
        const diff = c.cells.filter((x) => !aSet.has(x));
        const diffMines = c.mines - a.mines;
        if (diffMines === 0) diff.forEach((i) => toReveal.add(i));
        else if (diffMines === diff.length) diff.forEach((i) => toFlag.add(i));
      }
    }
  }
```

整体替换为:

```ts
  // 子集推理：A ⊆ B → (B−A) 的雷数 = B.mines − A.mines
  // 性能:A ⊆ C 必共享 A 的首格,只对共享首格的约束对检查;成员集每约束每轮建一次。
  // 推导结果与全配对检查完全一致(v2.2 规格 §1.3:规则集冻结,仅提速)。
  if (toReveal.size === 0 && toFlag.size === 0) {
    const sets = constraints.map((c) => new Set(c.cells));
    const byCell = new Map<number, number[]>();
    constraints.forEach((c, ci) => {
      for (const x of c.cells) {
        let list = byCell.get(x);
        if (!list) byCell.set(x, (list = []));
        list.push(ci);
      }
    });
    constraints.forEach((a, ai) => {
      for (const ci of byCell.get(a.cells[0]!) ?? []) {
        if (ci === ai) continue;
        const c = constraints[ci]!;
        if (a.cells.length >= c.cells.length) continue;
        const cSet = sets[ci]!;
        if (!a.cells.every((x) => cSet.has(x))) continue;
        const aSet = sets[ai]!;
        const diff = c.cells.filter((x) => !aSet.has(x));
        const diffMines = c.mines - a.mines;
        if (diffMines === 0) diff.forEach((i) => toReveal.add(i));
        else if (diffMines === diff.length) diff.forEach((i) => toFlag.add(i));
      }
    });
  }
```

- [ ] **Step 3: 回归验证**

Run: `npx vitest run tests/solver.test.ts tests/generator.test.ts && npx tsc --noEmit`
Expected: 全绿、tsc 无输出。generator.test 覆盖全部 50 关生成+可解断言,是语义不变的直接证据。

- [ ] **Step 4: Commit**

```bash
git add src/core/solver.ts
git commit -m "perf: 子集推理按共享首格剪枝,推导结果不变(v2.2 §1.3)" -m "Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

---

### Task 2: 修补式生成(甲)+ 28×44 探针

**Files:**
- Modify: `src/core/solver.ts`(isSolvable 拆出 solveProbe)
- Modify: `src/core/generator.ts`(整体重写为修补循环)
- Test: `tests/generator.test.ts`(追加 describe)、`tests/gen-bench.test.ts`(追加探针)

**Interfaces:**
- Consumes: `isSolvable`/`step`(Task 1 后的 solver)
- Produces: `solveProbe(board, firstIdx, maxComponent=24): ProbeResult`(solver.ts 新导出,`{ solved: boolean; revealed: boolean[] }`);`generate(level, firstIdx, rng, onSlow?)` 签名不变;`PERTURB_MAX = 50`(generator.ts 导出常量,防魔数、便于闸门调参)

- [ ] **Step 1: 写失败测试(generator.test.ts 末尾追加)**

```ts
describe("generate v2.2 修补式(规格 §1.2)", () => {
  it("同种子确定性:两次生成雷布局完全相同", () => {
    const level = LEVELS[9]!; // 14x20
    const first = Math.floor(level.height / 2) * level.width + Math.floor(level.width / 2);
    const a = generate(level, first, mulberry32(42));
    const b = generate(level, first, mulberry32(42));
    expect(a.mine).toEqual(b.mine);
  });

  it("28×44 终焉封顶规格可生成且无猜、盘面干净", () => {
    const big: LevelSpec = {
      id: 0, tier: "finale", width: 28, height: 44, mines: 285, timeLimitSec: 1800,
    };
    const first = Math.floor(44 / 2) * 28 + Math.floor(28 / 2);
    const b = generate(big, first, mulberry32(1));
    expect(b.mineCount).toBe(285);
    expect(b.revealed.some(Boolean)).toBe(false);
    expect(b.flagged.some(Boolean)).toBe(false);
    expect(isSolvable(b, first)).toBe(true);
  });
});
```

同文件顶部 import 行补 `type LevelSpec`:`import { LEVELS, TIER_NAMES, type Tier, type LevelSpec } from "../src/core/levels";`

- [ ] **Step 2: 跑测试确认现状**

Run: `npx vitest run tests/generator.test.ts -t "修补式"`
Expected: 确定性用例可能通过(现实现也确定),28×44 用例超时或极慢 —— 这就是要修的问题。若两者都快速通过则停下检查测试是否真的跑到了大盘。

- [ ] **Step 3: solver.ts 拆出 solveProbe**

把 `src/core/solver.ts` 的 `isSolvable`(连同其上方的文档注释)替换为:

```ts
export interface ProbeResult {
  solved: boolean;
  /** 停机(推完或卡死)时的已揭开快照,生成器修补用 */
  revealed: boolean[];
}

/**
 * 判定盘面能否从 firstIdx 出发全程纯逻辑推完（无猜）。
 * 推理层级：基础规则 → 子集推理 → 全局终局 → 边界穷举 + 全局雷数计数。
 * 不修改传入的 board。maxComponent 为穷举的连通块大小上限（0 = 禁用穷举）。
 */
export function isSolvable(board: Board, firstIdx: number, maxComponent = 24): boolean {
  return solveProbe(board, firstIdx, maxComponent).solved;
}

/** 与 isSolvable 同一推演,额外暴露停机时的已揭开集。不修改传入的 board。 */
export function solveProbe(board: Board, firstIdx: number, maxComponent = 24): ProbeResult {
  const size = board.width * board.height;
  const b: Board = {
    ...board,
    revealed: new Array<boolean>(size).fill(false),
    flagged: new Array<boolean>(size).fill(false),
  };
  if (b.mine[firstIdx]) return { solved: false, revealed: b.revealed };
  reveal(b, firstIdx);
  while (!isWin(b)) {
    if (!step(b, maxComponent)) return { solved: false, revealed: b.revealed };
  }
  return { solved: true, revealed: b.revealed };
}
```

- [ ] **Step 4: generator.ts 重写为修补循环**

`src/core/generator.ts` 全文替换为:

```ts
import { type Board, createBoard, neighbors } from "./board";
import { solveProbe } from "./solver";
import type { LevelSpec } from "./levels";

/** 单盘修补次数上限;超过则整盘作废重撒(防极端死循环) */
export const PERTURB_MAX = 50;

/**
 * 生成保证无猜的盘面(v2.2 修补式,设计文档 §1.2):
 * 随机布雷(首格 3×3 安全区无雷)→ 求解器从首格全程推演;卡死时不废盘,
 * 把全部雷收进「本轮未揭开的格子」重撒(已推开安全骨架保持无雷),再从首格从头重推。
 * 返回的盘必定完整通过了与 isSolvable 一字不差的推演——绝不降级。
 * 每累计 200 次推演回调一次 onSlow。
 */
export function generate(
  level: LevelSpec,
  firstIdx: number,
  rng: () => number,
  onSlow?: (attempts: number) => void,
): Board {
  const probe = createBoard(level.width, level.height, []);
  const excluded = new Set([firstIdx, ...neighbors(probe, firstIdx)]);
  const all: number[] = [];
  for (let i = 0; i < level.width * level.height; i++) {
    if (!excluded.has(i)) all.push(i);
  }

  let attempts = 0;
  for (;;) {
    let b = createBoard(level.width, level.height, sample(all, level.mines, rng));
    for (let p = 0; p <= PERTURB_MAX; p++) {
      attempts++;
      if (attempts % 200 === 0) {
        console.warn(`generate: level ${level.id} 已尝试 ${attempts} 次`);
        onSlow?.(attempts);
      }
      let r;
      try {
        r = solveProbe(b, firstIdx);
      } catch (e) {
        // 健全性哨兵触发意味着求解器缺陷；丢弃本盘整盘重来，保证不外泄错误盘
        console.error("generate: solver sentinel", e);
        break;
      }
      // solveProbe 在副本上推演,b 自创建后未被触碰,始终是未开局的干净盘
      if (r.solved) return b;
      const open = r.revealed;
      const candidates = all.filter((i) => !open[i]);
      if (candidates.length < level.mines) break; // 未开区放不下全部雷 → 整盘重来
      b = createBoard(level.width, level.height, sample(candidates, level.mines, rng));
    }
  }
}

/** 从 pool 中等概率抽取 n 个（部分 Fisher-Yates，不修改原数组） */
function sample(pool: number[], n: number, rng: () => number): number[] {
  const a = [...pool];
  for (let i = 0; i < n; i++) {
    const j = i + Math.floor(rng() * (a.length - i));
    [a[i], a[j]] = [a[j]!, a[i]!];
  }
  return a.slice(0, n);
}
```

- [ ] **Step 5: 跑新测试与全量回归**

Run: `npx vitest run tests/generator.test.ts tests/solver.test.ts && npx tsc --noEmit`
Expected: 全绿(含 28×44 用例,应在数百 ms 内完成)。

- [ ] **Step 6: gen-bench.test.ts 追加 28×44 探针**

在 `tests/gen-bench.test.ts` 末尾追加(import 行同步补 `type LevelSpec`):

```ts
// 修补式生成的目标规格探针:即使关卡表尚未调整,28×44 封顶盘也必须过闸门
describe.skipIf(!RUN)("修补式生成 28×44 探针(v2.2 规格 §1.4)", () => {
  it("终焉封顶规格 5 种子单盘 <500ms", () => {
    const big: LevelSpec = {
      id: 0, tier: "finale", width: 28, height: 44, mines: 285, timeLimitSec: 1800,
    };
    const first = Math.floor(big.height / 2) * big.width + Math.floor(big.width / 2);
    let worst = 0;
    for (const seed of [1, 2, 3, 4, 5]) {
      const t0 = performance.now();
      generate(big, first, mulberry32(seed));
      worst = Math.max(worst, performance.now() - t0);
    }
    appendFileSync("gen-bench.tmp.txt", `PROBE 28x44 worst=${worst.toFixed(0)}ms\n`);
    expect(worst).toBeLessThan(500);
  }, 30000);
});
```

- [ ] **Step 7: 跑探针闸门(关键验证点)**

Run(PowerShell): `$env:GEN_BENCH="1"; npx vitest run tests/gen-bench.test.ts -t "探针"; Remove-Item Env:GEN_BENCH`
Expected: PASS,查看 `gen-bench.tmp.txt` 记录 worst 值后删除该文件。
**若超 500ms:先调 PERTURB_MAX(30~80 试探)与剖析 solveProbe 热点,仍不达标则停下上报——这是规格 §1 的硬承诺,不许静默降级。**

- [ ] **Step 8: Commit**

```bash
git add src/core/solver.ts src/core/generator.ts tests/generator.test.ts tests/gen-bench.test.ts
git commit -m "feat: 修补式无猜生成——卡死只重撒未开区,28×44 过闸门(v2.2 §1)" -m "Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

---

### Task 3: 关卡表 21-50 恢复高瘦大盘

**Files:**
- Modify: `src/core/levels.ts:58-91`(21-50 行与注释)
- Test: 既有 `tests/levels.test.ts`(不变式已覆盖,无需改动)、`tests/generator.test.ts`、`tests/gen-bench.test.ts`

**Interfaces:**
- Consumes: Task 2 的修补式 `generate`
- Produces: 新 `LEVELS[20..49]`(id/tier 结构不变,仅 width/height/mines 变;timeLimitSec 不变)

- [ ] **Step 1: 替换 21-50 数据**

`src/core/levels.ts` 中注释块与 L21-L50 整体替换为(1-20 行一字不动):

```ts
  // ===== v2.2 关卡表(规格 §2):恢复高瘦大盘,终焉封顶 28×44=1232 格(修补式生成器保障
  // GEN_BENCH 闸门);格数/雷数严格递增,密度带 [22.1, 23.2] 内档均值缓升,全部 h≥w、
  // 宽高比约 1:1.4~1.65,限时 930s 起每关 +30s 至 1800s。
  // 任何调整必须同时通过 levels.test 不变式与 GEN_BENCH 闸门 =====
  { id: 21, tier: "inferno", width: 21, height: 34, mines: 158, timeLimitSec: 930 },
  { id: 22, tier: "inferno", width: 21, height: 35, mines: 163, timeLimitSec: 960 },
  { id: 23, tier: "inferno", width: 22, height: 34, mines: 166, timeLimitSec: 990 },
  { id: 24, tier: "inferno", width: 22, height: 35, mines: 171, timeLimitSec: 1020 },
  { id: 25, tier: "inferno", width: 22, height: 36, mines: 176, timeLimitSec: 1050 },
  { id: 26, tier: "inferno", width: 23, height: 35, mines: 180, timeLimitSec: 1080 },
  { id: 27, tier: "umbra", width: 23, height: 36, mines: 185, timeLimitSec: 1110 },
  { id: 28, tier: "umbra", width: 23, height: 37, mines: 190, timeLimitSec: 1140 },
  { id: 29, tier: "umbra", width: 24, height: 36, mines: 194, timeLimitSec: 1170 },
  { id: 30, tier: "umbra", width: 24, height: 37, mines: 200, timeLimitSec: 1200 },
  { id: 31, tier: "umbra", width: 25, height: 36, mines: 203, timeLimitSec: 1230 },
  { id: 32, tier: "umbra", width: 24, height: 38, mines: 206, timeLimitSec: 1260 },
  { id: 33, tier: "void", width: 25, height: 38, mines: 215, timeLimitSec: 1290 },
  { id: 34, tier: "void", width: 26, height: 37, mines: 218, timeLimitSec: 1320 },
  { id: 35, tier: "void", width: 25, height: 39, mines: 221, timeLimitSec: 1350 },
  { id: 36, tier: "void", width: 26, height: 38, mines: 224, timeLimitSec: 1380 },
  { id: 37, tier: "void", width: 26, height: 39, mines: 230, timeLimitSec: 1410 },
  { id: 38, tier: "void", width: 27, height: 38, mines: 234, timeLimitSec: 1440 },
  { id: 39, tier: "chaos", width: 26, height: 40, mines: 238, timeLimitSec: 1470 },
  { id: 40, tier: "chaos", width: 27, height: 39, mines: 241, timeLimitSec: 1500 },
  { id: 41, tier: "chaos", width: 26, height: 41, mines: 244, timeLimitSec: 1530 },
  { id: 42, tier: "chaos", width: 27, height: 40, mines: 247, timeLimitSec: 1560 },
  { id: 43, tier: "chaos", width: 27, height: 41, mines: 254, timeLimitSec: 1590 },
  { id: 44, tier: "chaos", width: 28, height: 40, mines: 257, timeLimitSec: 1620 },
  { id: 45, tier: "finale", width: 28, height: 41, mines: 264, timeLimitSec: 1650 },
  { id: 46, tier: "finale", width: 27, height: 43, mines: 267, timeLimitSec: 1680 },
  { id: 47, tier: "finale", width: 28, height: 42, mines: 271, timeLimitSec: 1710 },
  { id: 48, tier: "finale", width: 27, height: 44, mines: 274, timeLimitSec: 1740 },
  { id: 49, tier: "finale", width: 28, height: 43, mines: 278, timeLimitSec: 1770 },
  { id: 50, tier: "finale", width: 28, height: 44, mines: 285, timeLimitSec: 1800 },
];
```

(数表已按不变式预验算:格数 714→1232 严格递增、雷数 158→285 严格递增、密度全部 ∈[22.13, 23.14]、五档密度均值 22.22 < 22.47 < 22.69 < 22.90 < 23.05 严格递增且高于深渊档,全部 h≥w。)

- [ ] **Step 2: 跑不变式与全量生成回归**

Run: `npx vitest run tests/levels.test.ts tests/generator.test.ts && npx tsc --noEmit`
Expected: 全绿(generator.test 会对全部 50 关按种子矩阵实际生成并断言无猜,新大盘每盘应 <500ms)。

- [ ] **Step 3: 跑 GEN_BENCH 全量闸门**

Run(PowerShell): `$env:GEN_BENCH="1"; npx vitest run tests/gen-bench.test.ts; Remove-Item Env:GEN_BENCH`
Expected: 21-50 全关 + 探针 PASS;查看并记录 `gen-bench.tmp.txt` 的 worst 值(写进提交信息),然后删除该文件。

- [ ] **Step 4: Commit**

```bash
git add src/core/levels.ts
git commit -m "feat: 21-50 关恢复高瘦大盘,终焉封顶 28×44(v2.2 §2,GEN_BENCH 全绿)" -m "Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

---

### Task 4: 存档 v3(endless 字段 + 21-50 成绩迁移即弃)

**Files:**
- Modify: `src/core/storage.ts`
- Test: `tests/storage.test.ts`

**Interfaces:**
- Consumes: 无
- Produces: `SaveData`(version: 3,新增 `endless: { streak: number; bestStreak: number }`);`GameStorage` 新增 `recordEndlessWin(): EndlessWinRecord`(`{ streak: number; bestStreak: number; newBest: boolean }`)与 `recordEndlessLoss(): void`;导出 `interface EndlessWinRecord`

- [ ] **Step 1: 更新既有测试的期望值**

`tests/storage.test.ts` 机械修改(不动测试逻辑):
1. 所有期望对象里的 `version: 2` 改为 `version: 3`(共 6 处:空档默认、损坏 JSON 回退默认、backend 异常、v1 迁移的 `load()` 断言与 `JSON.parse` 持久化断言、v1 字段损坏);
2. 上述每个完整期望对象(`toEqual({...})` 与 `JSON.parse(...)` 断言)追加字段 `endless: { streak: 0, bestStreak: 0 }`;
3. 「损坏 JSON / 版本不符 / 非法字段回退默认」用例中 `'{"version":2,...'` 的两处输入存档字符串**保持 version 2 不变**(它们现在走 v2→v3 迁移路径,断言结果不变仍成立:损坏项回退、合法项保留);
4. 「缺省为 true(老 v2 档无此字段也回 true)」与「setSoundOn...非法值回退 true」用例的输入串不变。

- [ ] **Step 2: 追加新用例(storage.test.ts 末尾)**

```ts
describe("存档 v3(v2.2 规格 §3.4)", () => {
  it("v2→v3 迁移:1-20 成绩保留、21-50 丢弃、进度与静音保留、立即持久化", () => {
    const backend = memBackend({
      [SAVE_KEY]: JSON.stringify({
        version: 2,
        unlockedLevel: 43,
        bestTimes: { 1: 55, 20: 700, 21: 800, 35: 900, 50: 1200 },
        soundOn: false,
      }),
    });
    const s = createStorage(backend);
    expect(s.load()).toEqual({
      version: 3,
      unlockedLevel: 43,
      bestTimes: { 1: 55, 20: 700 },
      soundOn: false,
      endless: { streak: 0, bestStreak: 0 },
    });
    expect(JSON.parse(backend.map.get(SAVE_KEY)!)["version"]).toBe(3);
  });

  it("recordEndlessWin:连胜+1、破纪录判定、持久化", () => {
    const backend = memBackend();
    const s = createStorage(backend);
    expect(s.recordEndlessWin()).toEqual({ streak: 1, bestStreak: 1, newBest: true });
    expect(s.recordEndlessWin()).toEqual({ streak: 2, bestStreak: 2, newBest: true });
    expect(createStorage(backend).load().endless).toEqual({ streak: 2, bestStreak: 2 });
  });

  it("recordEndlessLoss:连胜归零、最长保留;再胜不破纪录直到超过", () => {
    const s = createStorage(memBackend());
    s.recordEndlessWin();
    s.recordEndlessWin();
    s.recordEndlessLoss();
    expect(s.load().endless).toEqual({ streak: 0, bestStreak: 2 });
    expect(s.recordEndlessWin()).toEqual({ streak: 1, bestStreak: 2, newBest: false });
    s.recordEndlessWin();
    expect(s.recordEndlessWin()).toEqual({ streak: 3, bestStreak: 3, newBest: true });
  });

  it("endless 字段损坏回退默认;bestStreak < streak 时自洽修正", () => {
    const bad = createStorage(
      memBackend({
        [SAVE_KEY]:
          '{"version":3,"unlockedLevel":1,"bestTimes":{},"soundOn":true,"endless":{"streak":-1,"bestStreak":"x"}}',
      }),
    );
    expect(bad.load().endless).toEqual({ streak: 0, bestStreak: 0 });
    const fix = createStorage(
      memBackend({
        [SAVE_KEY]:
          '{"version":3,"unlockedLevel":1,"bestTimes":{},"soundOn":true,"endless":{"streak":5,"bestStreak":2}}',
      }),
    );
    expect(fix.load().endless).toEqual({ streak: 5, bestStreak: 5 });
  });

  it("recordWin 与 setSoundOn 不影响 endless;endless 记录不影响进度", () => {
    const s = createStorage(memBackend());
    s.recordEndlessWin();
    s.recordWin(1, 60);
    s.setSoundOn(false);
    expect(s.load().endless).toEqual({ streak: 1, bestStreak: 1 });
    s.recordEndlessLoss();
    expect(s.load().unlockedLevel).toBe(2);
    expect(s.load().bestTimes[1]).toBe(60);
    expect(s.load().soundOn).toBe(false);
  });
});
```

- [ ] **Step 3: 跑测试确认失败**

Run: `npx vitest run tests/storage.test.ts`
Expected: FAIL(version 3 / endless 字段 / 新 API 均不存在)。

- [ ] **Step 4: 实现 storage.ts**

按序修改 `src/core/storage.ts`:

类型与默认值:

```ts
export interface EndlessData {
  streak: number;
  bestStreak: number;
}

export interface SaveData {
  version: 3;
  unlockedLevel: number;
  bestTimes: Record<number, number>;
  soundOn: boolean;
  endless: EndlessData;
}

export interface EndlessWinRecord {
  streak: number;
  bestStreak: number;
  newBest: boolean;
}

export interface GameStorage {
  load(): SaveData;
  save(d: SaveData): boolean;
  recordWin(levelId: number, timeSec: number): WinRecord;
  setSoundOn(on: boolean): boolean;
  recordEndlessWin(): EndlessWinRecord;
  recordEndlessLoss(): void;
}

function defaults(): SaveData {
  return {
    version: 3,
    unlockedLevel: 1,
    bestTimes: {},
    soundOn: true,
    endless: { streak: 0, bestStreak: 0 },
  };
}
```

`readFields` 末尾(`return d;` 之前)追加:

```ts
  const e = r["endless"];
  if (typeof e === "object" && e !== null) {
    const es = e as Record<string, unknown>;
    if (
      typeof es["streak"] === "number" &&
      Number.isInteger(es["streak"]) &&
      es["streak"] >= 0
    ) {
      d.endless.streak = es["streak"];
    }
    if (
      typeof es["bestStreak"] === "number" &&
      Number.isInteger(es["bestStreak"]) &&
      es["bestStreak"] >= 0
    ) {
      d.endless.bestStreak = es["bestStreak"];
    }
    // 自洽:最长连胜不得低于当前连胜
    if (d.endless.bestStreak < d.endless.streak) d.endless.bestStreak = d.endless.streak;
  }
```

`sanitize` 改为:

```ts
function sanitize(raw: unknown): SaveData {
  if (typeof raw !== "object" || raw === null) return defaults();
  const r = raw as Record<string, unknown>;
  if (r["version"] === 3) return readFields(r);
  if (r["version"] === 2) {
    // v2 迁移:进度/静音继承;21-50 盘面规格全变,旧成绩不可比,迁移即弃(规格 §3.4)
    const v2 = readFields(r);
    for (const k of Object.keys(v2.bestTimes)) {
      if (Number(k) >= 21) delete v2.bestTimes[Number(k)];
    }
    return v2;
  }
  if (r["version"] === 1) {
    // v1 迁移：进度继承；成绩仅保留盘面规格未变的关
    const v1 = readFields(r);
    const d = defaults();
    d.unlockedLevel = v1.unlockedLevel;
    for (const [k, t] of Object.entries(v1.bestTimes)) {
      const id = Number(k);
      const spec = V1_SPECS[id];
      const cur = LEVELS[id - 1];
      if (spec && cur && cur.width === spec[0] && cur.height === spec[1] && cur.mines === spec[2]) {
        d.bestTimes[id] = t;
      }
    }
    return d;
  }
  return defaults();
}
```

`createStorage` 内迁移持久化条件从 `=== 1` 扩为:

```ts
      const ver = (parsed as { version?: unknown } | null)?.["version"];
      if (ver === 1 || ver === 2) save(data); // 迁移结果立即持久化，避免下次再迁移
```

`load` 与既有写路径的深拷贝补 endless,并追加两个新方法(return 对象内):

```ts
    load: () => ({ ...data, bestTimes: { ...data.bestTimes }, endless: { ...data.endless } }),
```

`recordWin` 与 `setSoundOn` 中构造新对象处同样把 `endless: { ...data.endless }` 加进展开(共 2 处 `{ ...data, bestTimes: { ...data.bestTimes } }` 改为 `{ ...data, bestTimes: { ...data.bestTimes }, endless: { ...data.endless } }`),然后追加:

```ts
    recordEndlessWin() {
      const streak = data.endless.streak + 1;
      const newBest = streak > data.endless.bestStreak;
      const bestStreak = Math.max(data.endless.bestStreak, streak);
      save({ ...data, bestTimes: { ...data.bestTimes }, endless: { streak, bestStreak } });
      return { streak, bestStreak, newBest };
    },
    recordEndlessLoss() {
      save({
        ...data,
        bestTimes: { ...data.bestTimes },
        endless: { streak: 0, bestStreak: data.endless.bestStreak },
      });
    },
```

- [ ] **Step 5: 跑测试与全量回归**

Run: `npx vitest run tests/storage.test.ts && npx vitest run && npx tsc --noEmit`
Expected: storage 全绿;全量绿(home/ui 等只读新增字段,不受影响)。

- [ ] **Step 6: Commit**

```bash
git add src/core/storage.ts tests/storage.test.ts
git commit -m "feat: 存档 v3——endless 连胜字段,v2 迁移弃 21-50 旧成绩(v2.2 §3.4)" -m "Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

---

### Task 5: 无尽递进曲线 endless.ts + Tier 扩展

**Files:**
- Create: `src/core/endless.ts`
- Modify: `src/core/levels.ts`(Tier 联合 + TIER_NAMES)、`src/ui/style.css`(tier-endless 色)
- Modify: `tests/levels.test.ts:68-80`、`tests/generator.test.ts:42-54,58-69`(TIER_NAMES/SEEDS 补 endless)
- Test: Create `tests/endless.test.ts`

**Interfaces:**
- Consumes: `LevelSpec`/`Tier`(levels.ts)、`mulberry32`
- Produces: `endlessSpec(streak: number, rng: () => number): LevelSpec`(id 恒 0、tier 恒 "endless");导出常量 `ENDLESS_CAP_WINS=15`、`ENDLESS_MIN_CELLS=600`、`ENDLESS_MAX_CELLS=1232`、`ENDLESS_MIN_DENSITY=22.1`、`ENDLESS_MAX_DENSITY=23.2`、`ENDLESS_MIN_TIME=900`、`ENDLESS_MAX_TIME=2400`;`Tier` 新增 `"endless"`,`TIER_NAMES.endless === "无尽"`

- [ ] **Step 1: 写失败测试 tests/endless.test.ts**

```ts
import { describe, expect, it } from "vitest";
import { ENDLESS_MAX_CELLS, endlessSpec } from "../src/core/endless";
import { LEVELS } from "../src/core/levels";
import { mulberry32 } from "../src/core/rng";

describe("无尽递进曲线(v2.2 规格 §3.2)", () => {
  it("任意连胜数不超 L50 规格:格数≤1232、密度≤23.2%、h≥w、id=0、tier=endless", () => {
    const l50 = LEVELS[49]!;
    for (let streak = 0; streak <= 60; streak++) {
      for (const seed of [1, 2, 3]) {
        const s = endlessSpec(streak, mulberry32(seed));
        const cells = s.width * s.height;
        expect(cells).toBeLessThanOrEqual(l50.width * l50.height);
        expect((s.mines / cells) * 100).toBeLessThanOrEqual(23.2);
        expect(s.mines).toBeLessThanOrEqual(l50.mines);
        expect(s.height).toBeGreaterThanOrEqual(s.width);
        expect(s.mines).toBeGreaterThan(0);
        expect(s.id).toBe(0);
        expect(s.tier).toBe("endless");
      }
    }
  });

  it("固定抖动下标称曲线单调不减,第 15 胜起封顶恒定", () => {
    const at = (n: number) => endlessSpec(n, () => 0.5); // rng=0.5 → 抖动 0
    for (let n = 1; n <= 40; n++) {
      expect(at(n).width * at(n).height).toBeGreaterThanOrEqual(
        at(n - 1).width * at(n - 1).height,
      );
      expect(at(n).mines).toBeGreaterThanOrEqual(at(n - 1).mines);
      expect(at(n).timeLimitSec).toBeGreaterThanOrEqual(at(n - 1).timeLimitSec);
    }
    expect(at(15)).toEqual(at(30));
    expect(at(15).timeLimitSec).toBe(2400);
    expect(at(15).width * at(15).height).toBeLessThanOrEqual(ENDLESS_MAX_CELLS);
  });

  it("起步盘:约 600 格、密度 ≤22.1%、限时 900s", () => {
    const s = endlessSpec(0, () => 0.5);
    const cells = s.width * s.height;
    expect(cells).toBeGreaterThanOrEqual(560);
    expect(cells).toBeLessThanOrEqual(600);
    expect(s.timeLimitSec).toBe(900);
    expect((s.mines / cells) * 100).toBeLessThanOrEqual(22.1);
    expect((s.mines / cells) * 100).toBeGreaterThanOrEqual(21.5);
  });

  it("宽度随 rng 抖动 ±1,同连胜可得不同盘", () => {
    const widths = new Set([0, 0.5, 0.99].map((v) => endlessSpec(5, () => v).width));
    expect(widths.size).toBe(3);
  });
});
```

Run: `npx vitest run tests/endless.test.ts` → Expected: FAIL(模块不存在)。

- [ ] **Step 2: levels.ts 扩展 Tier**

`Tier` 联合追加 `| "endless"`(放在 `"finale"` 之后);`TIER_NAMES` 追加 `endless: "无尽",`。

- [ ] **Step 3: 修既有测试的 Record 穷尽性涟漪**

- `tests/levels.test.ts` 与 `tests/generator.test.ts` 的两处 `expect(TIER_NAMES).toEqual({...})` 对象各追加 `endless: "无尽",`;
- `tests/generator.test.ts` 的 `SEEDS: Record<Tier, number[]>` 追加 `endless: [],`。

- [ ] **Step 4: 实现 src/core/endless.ts**

```ts
import type { LevelSpec } from "./levels";

// 递进曲线常量(v2.2 设计文档 §3.2):第 ENDLESS_CAP_WINS 胜起到达封顶,此后恒定极限盘。
// 封顶规格 = 新 L50(1232 格、23.2%),GEN_BENCH 对 campaign 顶端的保障自动覆盖无尽全程。
export const ENDLESS_CAP_WINS = 15;
export const ENDLESS_MIN_CELLS = 600;
export const ENDLESS_MAX_CELLS = 1232;
export const ENDLESS_MIN_DENSITY = 22.1; // %
export const ENDLESS_MAX_DENSITY = 23.2; // %
export const ENDLESS_MIN_TIME = 900; // s
export const ENDLESS_MAX_TIME = 2400; // s
const CELLS_PER_WIN = 45;
const TIME_PER_WIN = 100;
const ASPECT = 1.5; // 高瘦 ~1:1.5

/** 无尽模式下一盘参数;streak 为当前连胜数(0 = 起步盘)。
 *  不变式(配 endless.test):固定抖动下曲线单调不减、封顶后恒定;
 *  实际盘面恒满足 格数≤1232、密度≤23.2%、h≥w。 */
export function endlessSpec(streak: number, rng: () => number): LevelSpec {
  const n = Math.max(0, Math.floor(streak));
  const cells = Math.min(ENDLESS_MIN_CELLS + CELLS_PER_WIN * n, ENDLESS_MAX_CELLS);
  const density =
    ENDLESS_MIN_DENSITY +
    ((ENDLESS_MAX_DENSITY - ENDLESS_MIN_DENSITY) * Math.min(n, ENDLESS_CAP_WINS)) /
      ENDLESS_CAP_WINS;
  const timeLimitSec = Math.min(ENDLESS_MIN_TIME + TIME_PER_WIN * n, ENDLESS_MAX_TIME);
  // 宽度 ±1 随机抖动;height 向下取整保证 w*h ≤ cells ≤ 1232
  const jitter = Math.floor(rng() * 3) - 1;
  const width = Math.max(8, Math.round(Math.sqrt(cells / ASPECT)) + jitter);
  const height = Math.max(width, Math.floor(cells / width));
  const mines = Math.floor(width * height * (density / 100));
  return { id: 0, tier: "endless", width, height, mines, timeLimitSec };
}
```

- [ ] **Step 5: style.css 补主题色**

在 `.tier-finale { ... }` 块之后追加:

```css
.tier-endless {
  --tier-color: #5b8fa8;
}
```

- [ ] **Step 6: 跑测试**

Run: `npx vitest run tests/endless.test.ts tests/levels.test.ts tests/generator.test.ts && npx tsc --noEmit`
Expected: 全绿。

- [ ] **Step 7: Commit**

```bash
git add src/core/endless.ts src/core/levels.ts src/ui/style.css tests/endless.test.ts tests/levels.test.ts tests/generator.test.ts
git commit -m "feat: 无尽递进曲线纯函数 + Tier 扩展 endless(v2.2 §3.2)" -m "Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

---

### Task 6: 视口净空数学(fit 用净空区、钳制用全屏区)

**Files:**
- Modify: `src/ui/viewport.ts`(Metrics/fitScale/clampView)
- Test: `tests/viewport.test.ts`(追加 describe)

**Interfaces:**
- Consumes: 无
- Produces: `Metrics` 新增可选 `insetTop?: number; insetBottom?: number`(缺省 0,行为与 v2.1 完全一致);`fitScale`/`clampView`/`zoomAt`/`maxScale` 签名不变

- [ ] **Step 1: 写失败测试(viewport.test.ts「视口数学」describe 末尾追加)**

```ts
  it("净空 fit:fitScale 按扣除上下净空后的高度计算", () => {
    const m: Metrics = { viewW: 600, viewH: 800, boardW: 300, boardH: 400, insetTop: 100, insetBottom: 100 };
    expect(fitScale(m)).toBe(1.5); // min(600/300, (800-200)/400)
    expect(fitScale({ ...m, insetTop: 0, insetBottom: 0 })).toBe(2); // 无净空回退 v2.1 行为
  });

  it("净空 clamp:盘小于净空区的轴向在净空区内居中", () => {
    const m: Metrics = { viewW: 600, viewH: 800, boardW: 300, boardH: 400, insetTop: 100, insetBottom: 100 };
    const v = clampView({ scale: 1, tx: -50, ty: 999 }, m);
    expect(v).toEqual({ scale: 1, tx: 150, ty: 200 }); // ty = insetTop + (600-400)/2
  });

  it("盘超出净空区:可延伸到全屏(含菜单底下),但净空区内不许露底", () => {
    const m: Metrics = { viewW: 600, viewH: 800, boardW: 300, boardH: 700, insetTop: 100, insetBottom: 100 };
    // bh=700 > 净空 600:ty ∈ [insetTop + 600 - 700, insetTop] = [0, 100]
    expect(clampView({ scale: 1, tx: 0, ty: 999 }, m).ty).toBe(100);
    expect(clampView({ scale: 1, tx: 0, ty: -999 }, m).ty).toBe(0);
    // 盘远大于全屏:上缘可越出屏幕顶端
    const big = { viewW: 600, viewH: 800, boardW: 300, boardH: 2000, insetTop: 100, insetBottom: 100 };
    expect(clampView({ scale: 1, tx: 0, ty: -9999 }, big).ty).toBe(100 + 600 - 2000); // -1300
  });

  it("净空过大(≥viewH)时回退按全高计算,不产生负可用区", () => {
    const m: Metrics = { viewW: 600, viewH: 800, boardW: 300, boardH: 400, insetTop: 500, insetBottom: 400 };
    expect(fitScale(m)).toBe(2); // 净空非法 → 按 viewH
  });
```

Run: `npx vitest run tests/viewport.test.ts` → Expected: 新 4 用例 FAIL(类型缺字段/行为不符)。

- [ ] **Step 2: 实现 viewport.ts**

`Metrics` 接口改为:

```ts
export interface Metrics {
  viewW: number;
  viewH: number;
  boardW: number;
  boardH: number;
  /** 上/下悬浮菜单净空(v2.2 §4.2):适配与居中避开,钳制允许延伸到其下。缺省 0。 */
  insetTop?: number;
  insetBottom?: number;
}
```

在 `fitScale` 之前加辅助函数,并改写 `fitScale`/`clampView`:

```ts
/** 扣除上下净空后的可用高度;净空非法(≥viewH)时回退全高 */
function innerViewH(m: Metrics): number {
  const ih = m.viewH - (m.insetTop ?? 0) - (m.insetBottom ?? 0);
  return ih > 0 ? ih : m.viewH;
}

/** 与 innerViewH 配套的可用区顶端 y;净空非法时回退 0 */
function innerTop(m: Metrics): number {
  const top = m.insetTop ?? 0;
  return m.viewH - top - (m.insetBottom ?? 0) > 0 ? top : 0;
}

export function fitScale(m: Metrics): number {
  const ih = innerViewH(m);
  if (m.viewW <= 0 || ih <= 0 || m.boardW <= 0 || m.boardH <= 0) return 1;
  return Math.min(m.viewW / m.boardW, ih / m.boardH);
}
```

```ts
/** 平移钳制：盘小于净空区的轴向在净空区内居中；大于净空区的轴向允许延伸到全屏边缘,
 *  但净空区内不许露底(v2.2 §4.2;无净空时与 v2.1 行为一致) */
export function clampView(v: ViewState, m: Metrics): ViewState {
  const bw = m.boardW * v.scale;
  const bh = m.boardH * v.scale;
  const top = innerTop(m);
  const ih = innerViewH(m);
  const tx = bw <= m.viewW ? (m.viewW - bw) / 2 : Math.min(0, Math.max(m.viewW - bw, v.tx));
  const ty = bh <= ih ? top + (ih - bh) / 2 : Math.min(top, Math.max(top + ih - bh, v.ty));
  return { scale: v.scale, tx, ty };
}
```

(`maxScale`/`zoomAt` 依赖 `fitScale`/`clampView`,自动继承新行为,不改。)

- [ ] **Step 3: 跑测试(新旧全部)**

Run: `npx vitest run tests/viewport.test.ts && npx tsc --noEmit`
Expected: 全绿——既有用例无净空,必须原样通过。

- [ ] **Step 4: Commit**

```bash
git add src/ui/viewport.ts tests/viewport.test.ts
git commit -m "feat: 视口净空数学——fit 用净空区、钳制放宽到全屏(v2.2 §4.2)" -m "Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

---

### Task 7: 游戏页全屏布局接线

**Files:**
- Modify: `src/ui/game.ts`(DOM 结构 + metrics)
- Modify: `src/ui/style.css`(.game/.game-top/.game-bottom/.board-viewport)
- Test: `tests/ui.test.ts`(追加 DOM 结构用例)

**Interfaces:**
- Consumes: Task 6 的 `Metrics.insetTop/insetBottom`
- Produces: 游戏页 DOM:`.game > (.game-top, .board-viewport, .game-bottom)`,`.pc-hint` 移入 `.game-bottom`,新增包裹行 `.bottom-actions`

- [ ] **Step 1: 写失败测试(ui.test.ts「游戏页」describe 末尾追加)**

```ts
  it("悬浮布局:提示行并入底栏,操作行有包裹容器", () => {
    start();
    expect(root.querySelector(".game-bottom .pc-hint")).not.toBeNull();
    expect(root.querySelector(".game-bottom .bottom-actions .mode-toggle")).not.toBeNull();
    expect(root.querySelector(".game-bottom .bottom-actions .restart")).not.toBeNull();
    expect(root.querySelector(".game > .board-viewport")).not.toBeNull();
  });
```

Run: `npx vitest run tests/ui.test.ts -t "悬浮布局"` → Expected: FAIL。

- [ ] **Step 2: game.ts DOM 调整**

`showGame` 中底栏构造段(`const bottom = ...` 到 `game.append(...)`)改为:

```ts
  const bottom = document.createElement("footer");
  bottom.className = "game-bottom";
  const bottomActions = document.createElement("div");
  bottomActions.className = "bottom-actions";
  const modeToggle = document.createElement("div");
  modeToggle.className = "mode-toggle";
  modeToggle.setAttribute("role", "group");
  modeToggle.setAttribute("aria-label", "点按模式");
  const digBtn = button("mode-btn active", "⛏ 挖开", () => setMode("dig"));
  const flagBtn = button("mode-btn", "🚩 插旗", () => setMode("flag"));
  modeToggle.append(digBtn, flagBtn);
  const restartBtn = button("pill restart", "↻ 重开", restart);
  bottomActions.append(modeToggle, restartBtn);

  const hint = document.createElement("p");
  hint.className = "pc-hint";
  hint.textContent = "左键挖开 · 右键插旗 · 滚轮缩放 · 拖动平移";
  bottom.append(bottomActions, hint);
  game.append(top, boardVp, bottom);
  root.replaceChildren(game);
```

`metrics()` 改为(上下栏 jsdom 下 offsetHeight=0,自动回退 v2.1 行为,测试保持确定性):

```ts
  function metrics(): Metrics {
    return {
      viewW: boardVp.clientWidth,
      viewH: boardVp.clientHeight,
      boardW,
      boardH,
      insetTop: top.offsetHeight,
      insetBottom: bottom.offsetHeight,
    };
  }
```

- [ ] **Step 3: style.css 布局改造**

`.game`/`.game-top`/`.board-viewport`/`.game-bottom`/`.pc-hint` 规则替换为(其余不动):

```css
.game {
  position: relative;
  width: 100%;
  height: 100dvh;
}

.game-top {
  position: absolute;
  z-index: 2;
  top: 0;
  left: 0;
  right: 0;
  margin: 0 auto;
  max-width: 35rem;
  display: flex;
  align-items: center;
  gap: 0.625rem;
  padding: max(0.875rem, env(safe-area-inset-top) + 0.375rem) 1rem 0.625rem;
}
```

```css
.board-viewport {
  position: absolute;
  inset: 0;
  overflow: hidden;
  touch-action: none;
}
```

```css
.game-bottom {
  position: absolute;
  z-index: 2;
  bottom: 0;
  left: 0;
  right: 0;
  margin: 0 auto;
  max-width: 35rem;
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 0.375rem;
  padding: 0.625rem 1rem calc(1rem + env(safe-area-inset-bottom));
}

.bottom-actions {
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 0.75rem;
}
```

`.pc-hint` 只删除外边距相关?现规则本就只有 display/文字样式,原样保留(位置由父级 flex 决定)。

- [ ] **Step 4: 跑全量测试**

Run: `npx vitest run && npx tsc --noEmit`
Expected: 全绿(既有变换/命中用例在 inset=0 下行为不变)。

- [ ] **Step 5: 浏览器人工快验(执行者自查,非用户验收)**

Run: `npm run dev`,浏览器打开,进入任一关:默认适配下棋盘完整可见不被栏遮挡;滚轮放大后棋盘延伸到窗口上下边缘、菜单浮在其上可点;缩小窗口高度重复检查。关闭 dev server。

- [ ] **Step 6: Commit**

```bash
git add src/ui/game.ts src/ui/style.css tests/ui.test.ts
git commit -m "feat: 游戏页全屏视口 + 上下栏绝对悬浮(v2.2 §4.1)" -m "Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

---

### Task 8: 液态玻璃质感 + 首页措辞小修

**Files:**
- Modify: `src/ui/style.css`(.pill/.mode-toggle/.mode-btn/.home-play/.home-select/.sound-btn + @supports 回退)
- Modify: `src/ui/home.ts`(「最快」→「最近通关」)
- Test: `tests/home.test.ts`(措辞断言)

**Interfaces:**
- Consumes: 无
- Produces: 纯视觉;`.home-stats` 文案含「最近通关」

- [ ] **Step 1: 措辞测试先行**

`tests/home.test.ts`「进行中」用例中 `expect(root.querySelector(".home-stats")!.textContent).toContain("0:45");` 之前插入一行:

```ts
    expect(root.querySelector(".home-stats")!.textContent).toContain("最近通关");
```

Run: `npx vitest run tests/home.test.ts` → Expected: FAIL。

- [ ] **Step 2: home.ts 改措辞**

`bestStat.textContent = `⏱ 最快 ${...}`` 行改为:

```ts
  bestStat.textContent = `⏱ 最近通关 ${bestOwner ? fmtTime(save.bestTimes[bestOwner.id]!) : "—"}`;
```

Run: `npx vitest run tests/home.test.ts` → Expected: PASS。

- [ ] **Step 3: style.css 液态玻璃(规格 §4.3 逐字)**

`.pill` 规则中 `background/border/border-radius/box-shadow` 四项替换,其余(padding/font/display 等)不动:

```css
.pill {
  border-radius: 999px;
  background: linear-gradient(135deg, rgba(255, 255, 255, 0.62), rgba(255, 255, 255, 0.34));
  backdrop-filter: blur(18px) saturate(185%);
  -webkit-backdrop-filter: blur(18px) saturate(185%);
  border: 1px solid rgba(255, 255, 255, 0.75);
  box-shadow:
    inset 0 1px 0 rgba(255, 255, 255, 0.95),
    inset 0 -1px 1px rgba(255, 255, 255, 0.28),
    0 4px 18px rgba(74, 69, 62, 0.16);
  padding: 0.5rem 0.875rem;
  font-size: 0.9375rem;
  display: inline-flex;
  align-items: center;
  gap: 0.375rem;
  transition: transform 0.15s ease;
}
```

`.mode-toggle` 同配方(保留原 padding/gap/display):

```css
.mode-toggle {
  display: none;
  border-radius: 999px;
  background: linear-gradient(135deg, rgba(255, 255, 255, 0.62), rgba(255, 255, 255, 0.34));
  backdrop-filter: blur(18px) saturate(185%);
  -webkit-backdrop-filter: blur(18px) saturate(185%);
  border: 1px solid rgba(255, 255, 255, 0.75);
  box-shadow:
    inset 0 1px 0 rgba(255, 255, 255, 0.95),
    inset 0 -1px 1px rgba(255, 255, 255, 0.28),
    0 4px 18px rgba(74, 69, 62, 0.16);
  padding: 0.25rem;
  gap: 0.25rem;
}
```

`.mode-btn` 圆角改胶囊、active 加高光:

```css
.mode-btn {
  border-radius: 999px;
  padding: 0.625rem 1.25rem;
  font-size: 0.97rem;
  color: var(--ink-soft);
  transition: background 0.15s ease, color 0.15s ease;
}

.mode-btn.active {
  background: rgba(242, 239, 233, 0.9);
  box-shadow: inset 0 1px 0 rgba(255, 255, 255, 0.8);
  color: var(--ink);
  font-weight: 600;
}
```

`.home-select` 与 `.sound-btn` 换白玻璃,规则整体替换为:

```css
.home-select {
  flex: 1;
  padding: 0.8125rem 0;
  background: linear-gradient(135deg, rgba(255, 255, 255, 0.62), rgba(255, 255, 255, 0.34));
  backdrop-filter: blur(18px) saturate(185%);
  -webkit-backdrop-filter: blur(18px) saturate(185%);
  border: 1px solid rgba(255, 255, 255, 0.75);
  box-shadow:
    inset 0 1px 0 rgba(255, 255, 255, 0.95),
    0 4px 18px rgba(74, 69, 62, 0.16);
  color: var(--ink);
  border-radius: 999px;
  font-size: 0.9375rem;
  transition: transform 0.15s ease;
}
```

```css
.sound-btn {
  font-size: 1.05rem;
  line-height: 1;
  padding: 0.25rem 0.375rem;
  background: linear-gradient(135deg, rgba(255, 255, 255, 0.62), rgba(255, 255, 255, 0.34));
  backdrop-filter: blur(18px) saturate(185%);
  -webkit-backdrop-filter: blur(18px) saturate(185%);
  border: 1px solid rgba(255, 255, 255, 0.75);
  box-shadow:
    inset 0 1px 0 rgba(255, 255, 255, 0.95),
    0 4px 18px rgba(74, 69, 62, 0.16);
  border-radius: 999px;
  transition: transform 0.15s ease;
}
```

`.home-play` 换着色玻璃(色相不变):

```css
.home-play {
  flex: 1.7;
  padding: 0.8125rem 0;
  background: linear-gradient(135deg, rgba(143, 174, 139, 0.88), rgba(143, 174, 139, 0.66));
  backdrop-filter: blur(18px) saturate(185%);
  -webkit-backdrop-filter: blur(18px) saturate(185%);
  border: 1px solid rgba(255, 255, 255, 0.45);
  color: #fff;
  border-radius: 999px;
  font-size: 1.0625rem;
  font-weight: 700;
  box-shadow:
    inset 0 1px 0 rgba(255, 255, 255, 0.55),
    0 6px 16px rgba(143, 174, 139, 0.4);
  transition: transform 0.15s ease;
}
```

文件末尾追加回退:

```css
/* ===== 液态玻璃回退(老浏览器) ===== */
@supports not ((backdrop-filter: blur(1px)) or (-webkit-backdrop-filter: blur(1px))) {
  .pill,
  .mode-toggle,
  .home-select,
  .sound-btn {
    background: rgba(255, 255, 255, 0.92);
  }
  .home-play {
    background: var(--easy);
  }
}
```

- [ ] **Step 4: 全量回归 + 浏览器快验**

Run: `npx vitest run && npx tsc --noEmit`
Expected: 全绿(纯 CSS,jsdom 不感知)。
`npm run dev` 浏览器过一遍首页/选关/游戏页,悬浮栏呈玻璃质感、透出棋盘色彩。

- [ ] **Step 5: Commit**

```bash
git add src/ui/style.css src/ui/home.ts tests/home.test.ts
git commit -m "feat: 液态玻璃质感全局统一 + 首页措辞最近通关(v2.2 §4.3)" -m "Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

---

### Task 9: 插旗音效 + 滚轮小修

**Files:**
- Modify: `src/ui/audio.ts`(playFlag/playUnflag)
- Modify: `src/ui/game.ts`(两处旗分支接线 + wheel 置空 downCellVi)
- Test: `tests/audio.test.ts`、`tests/ui.test.ts`

**Interfaces:**
- Consumes: audio.ts 既有 `tone`/`noise`/静音短路
- Produces: `playFlag(): void`、`playUnflag(): void`(audio.ts 导出)

- [ ] **Step 1: audio 失败测试(audio.test.ts describe 内追加)**

import 行补 `playFlag, playUnflag`。追加用例:

```ts
  it("插旗/拔旗音(甲·木钉入座):各 sine×2+噪声×1,拔旗低一档;静音短路", () => {
    unlock();
    const c = FakeCtx.instances[0]!;
    playFlag();
    expect(c.oscs).toHaveLength(2);
    expect(c.oscs.every((o) => o.type === "sine" && o.started)).toBe(true);
    expect(c.noises).toBe(1);
    playUnflag();
    expect(c.oscs).toHaveLength(4);
    expect(c.noises).toBe(2);
    setMuted(true);
    playFlag();
    playUnflag();
    expect(c.oscs).toHaveLength(4);
    expect(c.noises).toBe(2);
  });
```

Run: `npx vitest run tests/audio.test.ts` → Expected: FAIL(导出不存在)。

- [ ] **Step 2: audio.ts 实现(参数试听拍板,逐字实现,禁改)**

文件头注释首行改为:

```ts
// 七种音效的 Web Audio 实时合成(v2.1 设计文档 §3 + v2.2 §5,A·木质柔和)——零素材文件。
// 参数经用户试听拍板,修改前必须重新走试听确认。
```

文件末尾追加:

```ts
/** 插旗(v2.2 §5.1,甲·木钉入座) */
export function playFlag(): void {
  tone(988, "sine", 0.055, 0.2);
  tone(1976, "sine", 0.03, 0.05);
  noise(0.02, 0.07, 0, 1500);
}

/** 拔旗(低一档) */
export function playUnflag(): void {
  tone(784, "sine", 0.055, 0.18);
  tone(1568, "sine", 0.03, 0.04);
  noise(0.02, 0.05, 0, 1100);
}
```

Run: `npx vitest run tests/audio.test.ts` → Expected: PASS。

- [ ] **Step 3: ui 失败测试(ui.test.ts)**

`vi.mock("../src/ui/audio", ...)` 工厂对象追加 `playFlag: vi.fn(), playUnflag: vi.fn(),`。「游戏页」describe 末尾追加:

```ts
  it("旗音:预旗/盘上右键 插响拔响,拔旗低一档走 playUnflag", () => {
    const cells = start();
    press(cells[7]!, { button: 2 }); // 预旗
    expect(vi.mocked(audio.playFlag)).toHaveBeenCalledTimes(1);
    press(cells[7]!, { button: 2 }); // 预旗取消
    expect(vi.mocked(audio.playUnflag)).toHaveBeenCalledTimes(1);
    press(cells[7]!, { button: 2 }); // 重新预旗
    press(cells[63]!); // 首挖开局(预旗落盘不再响)
    expect(vi.mocked(audio.playFlag)).toHaveBeenCalledTimes(2);
    press(cells[7]!, { button: 2 }); // 盘上拔旗
    expect(vi.mocked(audio.playUnflag)).toHaveBeenCalledTimes(2);
    press(cells[7]!, { button: 2 }); // 盘上插旗
    expect(vi.mocked(audio.playFlag)).toHaveBeenCalledTimes(3);
  });

  it("旗音:触摸长按插旗同样响 playFlag", () => {
    const cells = start();
    press(cells[63]!, { touch: true }); // 开局
    const down = new MouseEvent("pointerdown", { bubbles: true, button: 0, clientX: 331, clientY: 30 });
    Object.defineProperty(down, "pointerType", { value: "touch" });
    cells[7]!.dispatchEvent(down);
    vi.advanceTimersByTime(400);
    const up = new MouseEvent("pointerup", { bubbles: true, button: 0, clientX: 331, clientY: 30 });
    Object.defineProperty(up, "pointerType", { value: "touch" });
    cells[7]!.dispatchEvent(up);
    expect(vi.mocked(audio.playFlag)).toHaveBeenCalledTimes(1);
  });

  it("按住期间滚轮缩放:抬起不挖格(v2.1 终审 Minor#3)", () => {
    const cells = start();
    const vp = root.querySelector<HTMLElement>(".board-viewport")!;
    const p = { x: 10 + 7 * 43 + 20, y: 10 + 7 * 43 + 20 }; // 63 号格心
    const fire = (type: string): void => {
      const e = new MouseEvent(type, { bubbles: true, button: 0, clientX: p.x, clientY: p.y });
      Object.defineProperty(e, "pointerType", { value: "mouse" });
      cells[63]!.dispatchEvent(e);
    };
    fire("pointerdown");
    vp.dispatchEvent(new WheelEvent("wheel", { deltaY: -100, bubbles: true, cancelable: true }));
    fire("pointerup");
    expect(root.querySelectorAll(".cell.open")).toHaveLength(0);
  });
```

Run: `npx vitest run tests/ui.test.ts -t "旗音"` 与 `-t "滚轮缩放:抬起"` → Expected: FAIL。

- [ ] **Step 4: game.ts 接线**

import 行补 `playFlag, playUnflag`(加入现有 audio import 大括号,按字母序)。

`act()` 预旗分支改为:

```ts
      if (action === "flag") {
        // 开局前预旗：只记标记，不生成盘面
        if (preFlags.has(liEarly)) {
          preFlags.delete(liEarly);
          playUnflag();
        } else {
          preFlags.add(liEarly);
          playFlag();
        }
        syncCell(i);
        updateStats();
        return;
      }
```

盘上旗分支改为(利用 `toggleFlag` 的返回值 = 切换后是否有旗):

```ts
    if (!b.revealed[li] && action === "flag") {
      if (toggleFlag(b, li)) playFlag();
      else playUnflag();
      syncCell(i);
      updateStats();
      return;
    }
```

wheel 处理器 `e.preventDefault();` 之后加一行:

```ts
      downCellVi = null; // 缩放改变盘格对位,按下格作废(v2.1 终审 Minor#3)
```

- [ ] **Step 5: 全量回归**

Run: `npx vitest run && npx tsc --noEmit`
Expected: 全绿(注意「插旗与预旗不出声」用例只断言挖格音静默,与旗音不冲突,应原样通过)。

- [ ] **Step 6: Commit**

```bash
git add src/ui/audio.ts src/ui/game.ts tests/audio.test.ts tests/ui.test.ts
git commit -m "feat: 插旗/拔旗音效(甲·木钉入座)四路径接线 + 滚轮中断点按(v2.2 §5)" -m "Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

---

### Task 10: 无尽模式接线(游戏页模式/结算/路由)

**Files:**
- Modify: `src/ui/game.ts`(GameDeps.mode + 标题)
- Modify: `src/ui/result.ts`(endless 变体)
- Test: `tests/ui.test.ts`

**Interfaces:**
- Consumes: 无(纯 UI 层;路由接线在 Task 11)
- Produces: `game.ts` 导出 `type GameMode = { kind: "campaign" } | { kind: "endless"; streak: number }`,`GameDeps` 新增可选 `mode?: GameMode`(缺省 campaign,现有调用零改动);`ResultOptions` 新增可选 `endless?: { streak: number }`

- [ ] **Step 1: 写失败测试(ui.test.ts)**

「游戏页」describe 追加:

```ts
  it("无尽模式:标题显示 ♾ 无尽与连胜徽章", () => {
    vi.useFakeTimers();
    Object.defineProperty(window, "innerWidth", { value: 400, configurable: true });
    Object.defineProperty(window, "innerHeight", { value: 800, configurable: true });
    showGame(root, {
      level,
      mode: { kind: "endless", streak: 3 },
      onExit: () => {},
      onFinish: () => {},
      onToggleSound: () => {},
    });
    const title = root.querySelector(".game-title")!;
    expect(title.textContent).toContain("无尽");
    expect(title.textContent).toContain("连胜 3");
    expect(title.querySelector(".tier-endless")).not.toBeNull();
    expect(title.textContent).not.toContain("第");
  });
```

「结算弹窗」describe 追加:

```ts
  it("无尽·胜:连胜标题、下一盘、最长连胜新纪录徽章、回首页", () => {
    let next = 0;
    let menu = 0;
    showResult({
      won: true, timeSec: 100, newBest: true, persisted: true, hasNext: true,
      endless: { streak: 7 },
      onNext: () => next++, onRetry: () => {}, onMenu: () => menu++,
    });
    const overlay = document.querySelector(".overlay")!;
    expect(overlay.textContent).toContain("连胜 7");
    expect(overlay.textContent).toContain("最长连胜");
    const buttons = [...overlay.querySelectorAll("button")].map((b) => b.textContent);
    expect(buttons).toContain("下一盘");
    expect(buttons).toContain("回首页");
    expect(buttons).not.toContain("下一关");
    [...overlay.querySelectorAll("button")].find((b) => b.textContent === "下一盘")!.click();
    expect(next).toBe(1);
  });

  it("无尽·负:连胜止于 N、再来一盘", () => {
    showResult({
      won: false, reason: "mine", timeSec: 50, newBest: false, persisted: true, hasNext: false,
      endless: { streak: 4 },
      onNext: () => {}, onRetry: () => {}, onMenu: () => {},
    });
    const overlay = document.querySelector(".overlay")!;
    expect(overlay.textContent).toContain("连胜止于 4");
    const buttons = [...overlay.querySelectorAll("button")].map((b) => b.textContent);
    expect(buttons).toContain("再来一盘");
    expect(buttons).toContain("回首页");
  });
```

Run: `npx vitest run tests/ui.test.ts -t "无尽"` → Expected: FAIL。

- [ ] **Step 2: game.ts 模式与标题**

`GameDeps` 上方加导出类型,接口内加字段:

```ts
export type GameMode = { kind: "campaign" } | { kind: "endless"; streak: number };

export interface GameDeps {
  level: LevelSpec;
  mode?: GameMode;
  onExit(): void;
  onFinish(result: GameResult): void;
  onToggleSound(on: boolean): void;
}
```

`showGame` 内 `title.innerHTML = ...` 一行改为:

```ts
  const mode: GameMode = deps.mode ?? { kind: "campaign" };
  title.innerHTML =
    mode.kind === "endless"
      ? `<b>♾ 无尽</b><span class="game-tier tier-endless">连胜 ${mode.streak}</span>`
      : `<b>第 ${level.id} 关</b><span class="game-tier tier-${level.tier}">${TIER_NAMES[level.tier]}</span>`;
```

(重开走 `showGame(root, deps)` 原样带 mode,天然正确。)

- [ ] **Step 3: result.ts endless 变体**

`ResultOptions` 加字段:

```ts
  /** 无尽模式:胜=新连胜数,负=止步时的连胜数;存在即启用无尽文案 */
  endless?: { streak: number };
```

标题/徽章/按钮段改为:

```ts
  const icon = opts.won ? (opts.endless ? "🔥" : "🎉") : opts.reason === "time" ? "⏰" : "💥";
  const heading = opts.endless
    ? opts.won
      ? `连胜 ${opts.endless.streak}!`
      : `连胜止于 ${opts.endless.streak}`
    : opts.won
      ? "通关！"
      : opts.reason === "time"
        ? "时间到"
        : "踩到雷了";
```

新纪录徽章行改为:

```ts
    badge.textContent = opts.endless ? "★ 新纪录 · 最长连胜" : "★ 新纪录";
```

按钮段改为:

```ts
  if (opts.won) {
    if (opts.endless) {
      actions.appendChild(btn("btn primary win", "下一盘", close(opts.onNext)));
    } else {
      if (opts.hasNext) actions.appendChild(btn("btn primary win", "下一关", close(opts.onNext)));
      actions.appendChild(btn(opts.hasNext ? "btn" : "btn primary win", "重玩", close(opts.onRetry)));
    }
  } else {
    actions.appendChild(btn("btn primary lose", opts.endless ? "再来一盘" : "重试", close(opts.onRetry)));
  }
  actions.appendChild(btn("btn", opts.endless ? "回首页" : "返回选关", close(opts.onMenu)));
```

- [ ] **Step 4: 全量回归**

Run: `npx vitest run && npx tsc --noEmit`
Expected: 全绿。

- [ ] **Step 5: Commit**

```bash
git add src/ui/game.ts src/ui/result.ts tests/ui.test.ts
git commit -m "feat: 无尽模式 UI——游戏页模式标识与结算变体(v2.2 §3.5)" -m "Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

---

### Task 11: 首页无尽入口与最长连胜

**Files:**
- Modify: `src/ui/home.ts`(onEndless + 按钮 + 统计)
- Modify: `src/main.ts`(gotoHome 传 onEndless)
- Modify: `src/ui/style.css`(.home-endless/.he-sub)
- Test: `tests/home.test.ts`

**Interfaces:**
- Consumes: Task 4 `recordEndlessWin/recordEndlessLoss`、Task 5 `endlessSpec`、Task 10 `GameMode`/`ResultOptions.endless`
- Produces: `HomeDeps` 新增 `onEndless(): void`;DOM 新增 `.home-endless`(锁定态含 `.he-sub`);`main.ts` 内部 `gotoEndless()` 完整接线

- [ ] **Step 1: 写失败测试(home.test.ts)**

`show()` 辅助函数改为(加 onEndless 计数):

```ts
function show(storage = createStorage(memBackend()), over: Partial<Parameters<typeof showHome>[1]> = {}) {
  const played: LevelSpec[] = [];
  let selected = 0;
  let endless = 0;
  showHome(root, {
    storage,
    version: "9.9.9-test",
    onContinue: (l) => played.push(l),
    onSelect: () => selected++,
    onEndless: () => endless++,
    ...over,
  });
  return { played, get selected() { return selected; }, get endless() { return endless; }, storage };
}
```

describe 末尾追加:

```ts
  it("无尽入口:未通关 50 关时灰态锁定并示明条件", () => {
    const t = show();
    const btn = root.querySelector<HTMLButtonElement>(".home-endless")!;
    expect(btn.disabled).toBe(true);
    expect(btn.textContent).toContain("无尽");
    expect(btn.textContent).toContain("通关 50 关解锁");
    btn.click();
    expect(t.endless).toBe(0);
  });

  it("无尽入口:通关 50 关后可点,触发 onEndless", () => {
    const storage = createStorage(memBackend());
    for (const l of LEVELS) storage.recordWin(l.id, 100);
    const t = show(storage);
    const btn = root.querySelector<HTMLButtonElement>(".home-endless")!;
    expect(btn.disabled).toBe(false);
    btn.click();
    expect(t.endless).toBe(1);
  });

  it("最长连胜 >0 时统计行显示", () => {
    const storage = createStorage(memBackend());
    storage.recordEndlessWin();
    storage.recordEndlessWin();
    show(storage);
    expect(root.querySelector(".home-stats")!.textContent).toContain("最长连胜 2");
    show(createStorage(memBackend())); // 无纪录不显示
    expect(root.querySelector(".home-stats")!.textContent).not.toContain("最长连胜");
  });
```

Run: `npx vitest run tests/home.test.ts` → Expected: FAIL(onEndless 类型报错 / .home-endless 不存在)。

- [ ] **Step 2: home.ts 实现**

`HomeDeps` 加 `onEndless(): void;`。`stats.append(doneStat, bestStat, soundBtn);` 改为:

```ts
  stats.append(doneStat, bestStat);
  if (save.endless.bestStreak > 0) {
    const streakStat = document.createElement("span");
    streakStat.textContent = `♾ 最长连胜 ${save.endless.bestStreak}`;
    stats.appendChild(streakStat);
  }
  stats.appendChild(soundBtn);
```

`actions.append(playBtn, selBtn);` 之前加:

```ts
  const endlessBtn = document.createElement("button");
  endlessBtn.type = "button";
  endlessBtn.className = "home-endless";
  if (cleared) {
    endlessBtn.textContent = "♾ 无尽";
    endlessBtn.addEventListener("click", () => deps.onEndless());
  } else {
    endlessBtn.disabled = true;
    endlessBtn.classList.add("locked");
    endlessBtn.innerHTML = `♾ 无尽<span class="he-sub">🔒 通关 50 关解锁</span>`;
  }
```

并把 append 行改为 `actions.append(playBtn, selBtn, endlessBtn);`

- [ ] **Step 3: main.ts 路由与接线**

import 补:`import { endlessSpec } from "./core/endless";` 与 `import { mulberry32 } from "./core/rng";`。`gotoGame` 之后追加:

```ts
function gotoEndless(): void {
  const streak = storage.load().endless.streak;
  const level = endlessSpec(streak, mulberry32((Math.random() * 2 ** 32) >>> 0));
  showGame(root, {
    level,
    mode: { kind: "endless", streak },
    onExit: gotoHome, // 中途退出:本局不计、连胜保留(规格 §3.3)
    onToggleSound: (on) => void storage.setSoundOn(on),
    onFinish: (result) => {
      if (result.won) {
        const rec = storage.recordEndlessWin();
        showResult({
          won: true,
          timeSec: result.timeSec,
          newBest: rec.newBest,
          persisted: true,
          hasNext: true,
          endless: { streak: rec.streak },
          onNext: gotoEndless,
          onRetry: gotoEndless,
          onMenu: gotoHome,
        });
      } else {
        const ended = storage.load().endless.streak;
        storage.recordEndlessLoss();
        showResult({
          won: false,
          reason: result.reason,
          timeSec: result.timeSec,
          newBest: false,
          persisted: true,
          hasNext: false,
          endless: { streak: ended },
          onNext: gotoEndless,
          onRetry: gotoEndless, // 再来一盘:连胜已归零,回起步盘
          onMenu: gotoHome,
        });
      }
    },
  });
}
```

并在 `gotoHome` 的 `showHome` deps 中加 `onEndless: gotoEndless,`。

- [ ] **Step 4: style.css**

`.home-select` 规则后追加:

```css
.home-endless {
  flex: 1;
  padding: 0.8125rem 0;
  background: linear-gradient(135deg, rgba(255, 255, 255, 0.62), rgba(255, 255, 255, 0.34));
  backdrop-filter: blur(18px) saturate(185%);
  -webkit-backdrop-filter: blur(18px) saturate(185%);
  border: 1px solid rgba(255, 255, 255, 0.75);
  box-shadow:
    inset 0 1px 0 rgba(255, 255, 255, 0.95),
    0 4px 18px rgba(74, 69, 62, 0.16);
  color: var(--ink);
  border-radius: 999px;
  font-size: 0.9375rem;
  transition: transform 0.15s ease;
}

.home-endless:active:not(:disabled) {
  transform: scale(0.97);
}

.home-endless.locked {
  opacity: 0.55;
  cursor: default;
}

.he-sub {
  display: block;
  font-size: 0.6rem;
  color: var(--ink-soft);
  line-height: 1.3;
}
```

并在 Task 8 的 @supports 回退块中把 `.home-endless` 加入白玻璃回退选择器列表。

- [ ] **Step 5: 全量回归**

Run: `npx vitest run && npx tsc --noEmit`
Expected: 全绿。

- [ ] **Step 6: Commit**

```bash
git add src/ui/home.ts src/main.ts src/ui/style.css tests/home.test.ts
git commit -m "feat: 首页无尽入口(通关 50 解锁)与最长连胜统计(v2.2 §3.1)" -m "Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

---

### Task 12: 版本 2.2.0 + 全量验收 + 桌面打包

**Files:**
- Modify: `src/main.ts:10`(APP_VERSION)、`vite.config.ts:15`(manifest 描述)、`package.json`、`src-tauri/tauri.conf.json`、`src-tauri/Cargo.toml`(版本号,以 grep 结果为准)

**Interfaces:**
- Consumes: 全部前序任务
- Produces: v2.2.0 发布件(dist + NSIS 安装包)

- [ ] **Step 1: 版本号与文案**

1. `src/main.ts`:`const APP_VERSION = "2.2.0";`
2. `vite.config.ts`:manifest `description: "无猜扫雷 · 五十关十档 · 无尽",`
3. 检索其余版本号:`git grep -n "2\.1\.0" -- package.json src-tauri/tauri.conf.json src-tauri/Cargo.toml src-tauri/Cargo.lock`,把 package.json / tauri.conf.json / Cargo.toml 中的 `2.1.0` 改为 `2.2.0`(Cargo.lock 由构建自动更新,不手改)。

- [ ] **Step 2: 全量测试 + 类型 + 闸门**

Run:
```
npx vitest run
npx tsc --noEmit
```
Expected: 全绿(预期 160+ passed | 30 skipped 量级)、tsc 无输出。

Run(PowerShell): `$env:GEN_BENCH="1"; npx vitest run tests/gen-bench.test.ts; Remove-Item Env:GEN_BENCH`
Expected: 31 用例全 PASS;记录 `gen-bench.tmp.txt` worst 值后删除。

- [ ] **Step 3: 网页构建冒烟**

Run: `npm run build && npm run preview`
浏览器打开 preview 地址:首页(液态玻璃、无尽入口)→ 选关 → 进游戏(悬浮栏、旗音)→ 退出。关闭 preview。

- [ ] **Step 4: 桌面打包**

新 shell 先刷新 PATH(`$env:Path = [System.Environment]::GetEnvironmentVariable("Path","Machine") + ";" + [System.Environment]::GetEnvironmentVariable("Path","User")`),然后 `npx tauri build`。
构建后:`git diff --ignore-cr-at-eol -- src-tauri/Cargo.toml` 为空则 `git restore src-tauri/Cargo.toml`(EOL 噪音);`src-tauri/Cargo.lock` 的版本号变更**保留提交**。
把 `src-tauri/target/release/bundle/nsis/minesweeper_2.2.0_x64-setup.exe` 复制到仓库根(根目录 `*-setup.exe` 已 gitignore),运行安装包冒烟 5 秒确认可启动。

- [ ] **Step 5: Commit**

```bash
git add src/main.ts vite.config.ts package.json src-tauri/tauri.conf.json src-tauri/Cargo.toml src-tauri/Cargo.lock
git commit -m "chore: 版本 2.2.0 + manifest 无尽文案 + 桌面打包" -m "Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

- [ ] **Step 6: 交付清单(报告给用户,不自行合并)**

汇总:测试数、GEN_BENCH worst 值、安装包路径与字节数;提请用户手动验收(iPhone PWA 液态玻璃/悬浮菜单/旗音/无尽全流程、老存档迁移、桌面 exe),并走 finishing-a-development-branch 流程决定合并发布。
