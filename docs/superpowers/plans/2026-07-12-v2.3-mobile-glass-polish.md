# v2.3 Mobile Liquid Glass Polish Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 让首页和游戏顶栏匹配 iPhone 截图中的胶囊玻璃与对称移动布局。

**Architecture:** 仅调整 `src/ui/style.css` 的响应式布局与触控尺寸，沿用现有 `game.ts` DOM。先扩展 `tests/style.test.ts` 的失败断言，再补 CSS；用现有 E2E 与移动视口冒烟验证。

**Tech Stack:** TypeScript, CSS, Vitest, Playwright.

---

### Task 1: Add regression assertions

**Files:**
- Modify: `tests/style.test.ts`

- [ ] **Step 1: Write assertions for capsule panel and balanced top controls**

断言 `.home-panel` 使用 `3rem` 圆角，`.game-top`/`.top-actions` 使用窄屏 grid 规则，`.back`/`.game-sound`/`.restart` 的最小尺寸为 44px。

- [ ] **Step 2: Run the focused test and confirm it fails**

Run: `npx vitest run tests/style.test.ts`
Expected: FAIL because the current candidate still has the old radius/grid declarations.

### Task 2: Implement the responsive polish

**Files:**
- Modify: `src/ui/style.css`

- [ ] **Step 1: Apply the minimal CSS changes**

Use the existing glass surface and DOM. Add grid placement for `.top-actions`, make `.game-stats` an equal-width control row on narrow screens, increase icon readability, and keep all interactive controls at least 44px.

- [ ] **Step 2: Run focused tests**

Run: `npx vitest run tests/style.test.ts`
Expected: PASS with no new failures.

### Task 3: Verify behavior and release readiness

**Files:**
- No additional source files.

- [ ] **Step 1: Run unit tests**

Run: `npm test`

- [ ] **Step 2: Run browser tests**

Run: `npm run test:e2e`

- [ ] **Step 3: Build and check diff**

Run: `npm run build:web; git diff --check`

- [ ] **Step 4: Review the final diff before committing**

Confirm only the intended CSS, test, spec and plan files changed; do not tag or release until physical iPhone verification is approved.
